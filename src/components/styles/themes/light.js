import siteConfig from '../../../../data/siteConfig'

export default {
  colors: {
    background: siteConfig.background_color,
    backgroundLight: siteConfig.background_light,
    primary: siteConfig.theme_color,
    fontColor: siteConfig.fontColor,
  },
}
